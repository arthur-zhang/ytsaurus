# Table Chunk Format - 表数据块格式

本模块定义 YTsaurus 表数据块（Table Chunk）的存储格式和序列化规范。

## 核心功能

1. **数据格式定义**：定义表数据在 Chunk 中的存储格式
2. **序列化/反序列化**：提供数据格式的编解码功能
3. **压缩支持**：支持多种压缩算法
4. **版本管理**：管理不同版本的格式规范

## 格式特点

- 高效的列式存储
- 优化的压缩算法
- 支持稀疏数据
- 向后兼容性保证

该格式是 YTsaurus 存储引擎的基础，确保数据的高效存储和快速访问。